{
  "name": "organiza-plus",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:security": "node --test tests/security.test.js",
    "test:flow": "node tests/approval-flow.e2e.mjs",
    "serve": "node scripts/serve.mjs",
    "serve:dist": "node scripts/serve.mjs --dist",
    "build": "node build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
